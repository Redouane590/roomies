<div class="container">
<% @lists.each do |list| %>
  <div class="list-index">
  <% list.colocation = @colocation %>
  <%= link_to colocation_list_path(@colocation, list) do%>
    <%= list.title %>
  <% end %>
  <%= link_to colocation_list_path(@colocation, list), data: {turbo_method: :delete, turbo_confirm: "Etes-vous sûr.e ?"} do %>
    <i class="fa-solid fa-trash"></i>
  <% end %> <br>
  <% list.items.first(3).each do |item| %>
  <%= item.name %> <br>
  <% end %>
</div>
<% end %>

<%= link_to "Nouvelle liste", new_colocation_list_path %>
</div>
