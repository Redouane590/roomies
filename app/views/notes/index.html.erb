<div class="container text-center" data-controller="notes">
  <h1>NOTES IMPORTANTES</h1>
  <div class="parent" id="notes">
    <% @notes.each do |note| %>
    <%= render "note", note: note %>
    <% end %>
  </div>

  <div class="container d-none " data-notes-target="display" style="margin-bottom: 200px;">
    <h2 class="text-center">Creer votre note</h1>

      <%= simple_form_for [@colocation, @note], html: {

    data: {
      notes_target: "form",
      action: "submit->notes#send"
    } } do |f| %>

      <%= f.input :title, :label => "Titre de la note"  %>
      <%= f.input :content, :label => "Contenu de la note" %>
      <%= f.button :submit, "Ajouter la note", class: "btn btn-primary " %>
      <a href="" class="btn btn-danger" data-notes-target="annuler" data-action="click->notes#annuler">Annuler</a>
      <%= link_to "Annuler", colocation_notes_path, class: "btn btn-danger" %>
      <% end %>

  </div>
  <div><a>
      <i class="fa-solid fa-circle-plus button-add-yellow" data-notes-target="button"
        data-action="click->notes#addNotes"></i>
    </a>
    <p class="d-none" style="margin-bottom: 100px;" data-notes-target="error">Vous ne pouvez plus ajouter de notes, max:
      10notes</p>
  </div>


</div>
